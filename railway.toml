[build]
builder = "nixpacks"
buildCommand = "pip install -r backtest-app/backend/requirements.txt"

[deploy]
startCommand = "cd backtest-app/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
